apiVersion: v1
kind: Service
metadata:
  name: seleniumtestdemoapp-service
spec:
  selector:
    app: seleniumtestdemoapp
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30188
  type: NodePort